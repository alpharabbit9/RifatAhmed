export default function AdminDashboardPage() {
  return (
    <div>
      <p className="label-overline">Dashboard</p>
      <h1 className="mt-2 font-display text-3xl text-foreground">
        Welcome back
      </h1>
      <p className="mt-2 max-w-md text-foreground-muted">
        Section management screens will appear here as each site section is
        built, starting with Hero.
      </p>
    </div>
  );
}
